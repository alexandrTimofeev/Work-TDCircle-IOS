using System.Collections;
using UnityEngine;

namespace Assets._Tamplates.Utils
{
    public class Unparrent : MonoBehaviour
    {

        // Use this for initialization
        void Start()
        {
            transform.parent = null;

        }

        // Update is called once per frame
        void Update()
        {
        }
    }
}